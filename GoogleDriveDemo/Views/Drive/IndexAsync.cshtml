@{
    ViewBag.Title = "Google Drive Data";
}

<h2>@ViewBag.Title</h2>

@for (var i = 0; i < ViewBag.FileNames.Length; i++)
{
    <p>@string.Format("{0}. {1}", i + 1, ViewBag.FileNames[i])</p>
}

<h3>@ViewBag.Message</h3>